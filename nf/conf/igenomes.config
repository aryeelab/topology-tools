/*
========================================================================================
    iGenomes Configuration for Micro-C Pipeline
========================================================================================
    Minimal iGenomes configuration file.
    This pipeline doesn't use iGenomes references, so this is a placeholder.
========================================================================================
*/

params {
    // iGenomes configuration
    igenomes_ignore = true
    genomes = [:]
    
    // Genome-specific parameters (empty for this pipeline)
    genome = null
    fasta = null
    gtf = null
    gff = null
    
    // Custom genome configuration can be added here if needed
    // For this pipeline, users provide their own BWA index and chromosome sizes
}
